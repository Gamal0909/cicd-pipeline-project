kind: Service
apiVersion: v1
metadata:
  name: cicd-demo
spec:
  type: NodePort
  selector:
    app: cicd-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30548
